# Workflow Hardening Research Journal (Archived)

## Status

This document is archived historical research material.

It references exploratory scripts and intermediate workflow lanes that were
later consolidated into the canonical `./run` and CI workflows.

## Current Execution Paths

Use these maintained entry points instead of historical script references:

- `./run gate`
- `./run gate lint`
- `./run gate test`
- `./run verify formal ...`
- `./run verify mcp ...`

## Current Validation Artifacts

- `tests/artifacts/workflow/`
- `tests/artifacts/algorithm/`

## Archival Note

Historical command/file references from this journal were intentionally pruned
because they no longer map to active scripts in `scripts/`.
